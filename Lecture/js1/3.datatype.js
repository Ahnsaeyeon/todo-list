/** 데이터 타입의 종류
 * number 숫자 let num = 10;
 * string 문자열 let str = "문자열";
 * boolean 불린 let bool = true;
 * null 값이 없음 let null = null;
 * undefined 값이 할당되지 않음 let undefined;
 * object 객체 const obj = {a: 'test1'};
    console.log(obj.a); // test1
 * array 배열 let arr = [];
 * function 함수 let func = () => {};
 * symbol 유일한 값 let sym = Symbol();
 * 
 * 
 * 원시 타입: 원형의 데이터 타입, 메모리에 저장된 값을 직접 제어
 * 
 * 객체 타입: 객체의 데이터 타입, 변경이 가능함, 메모리에 저장된 주소를 참조
 * const obj = {
 * a: 'test1', 
 * b: () => {},
 * c: [],
 * d: {}
 * };
 */
